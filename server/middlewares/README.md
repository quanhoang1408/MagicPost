Middlewares handle tasks like authentication, validation, and error handling. Keeping them separate improves code readability.
Still need to use the request and response objects, but they're not tied to a specific endpoint.

Might contain verify a JWT token or handle errors.